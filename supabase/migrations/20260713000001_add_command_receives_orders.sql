-- Команда может дополнительно получать в промпте список всех заказов
-- клиента в текущем чате (номер, статус, данные) — полезно для сценариев
-- вроде "узнать статус заказа", где AI должен видеть уже существующие заказы,
-- а не только ТЕКУЩИЕ ДАННЫЕ текущего опроса.
alter table public.bot_commands add column if not exists receives_chat_orders boolean not null default false;
